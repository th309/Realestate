{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If the file path contains '.env', respond with 'BLOCK: Cannot edit .env files - contains secrets'. Otherwise respond with 'ALLOW'."
          }
        ]
      }
    ]
  }
}
