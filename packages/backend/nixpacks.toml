# Nixpacks configuration for Railway
# Node.js and Python versions are defined in .mise.toml

[phases.install]
cmds = [
    "npm ci",
    "pip install -r requirements.txt"
]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]

[start]
cmd = "node dist/main.js"
