# Nixpacks configuration for Railway
# Adds Python support alongside Node.js for ML workflow scripts

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = [
    "npm ci",
    "pip install -r requirements.txt"
]
