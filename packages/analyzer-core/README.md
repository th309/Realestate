# @propertyiq/analyzer-core

Pure TypeScript math and modeling library for PropertyIQ's real-estate deal analyzer. Provides framework-agnostic primitives (cash flow, cap rate, IRR, amortization, sensitivity) consumed by the backend API, MCP server tools, and frontend UI. No I/O, no framework dependencies — all functions are deterministic and unit-testable in isolation.
