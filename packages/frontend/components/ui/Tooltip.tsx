// Thin re-export shim — the implementation was split into tooltip-parts/
// (one component per file, per CLAUDE.md file-size limits). Kept at this
// path so existing imports of "@/components/ui/Tooltip" don't need to change.
export * from "./tooltip-parts";
