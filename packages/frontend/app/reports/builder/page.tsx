import { Builder } from './Builder';

export const metadata = {
  title: 'Report Builder | PropertyIQ',
  description: 'Create custom reports with our drag-and-drop builder',
};

export default function ReportBuilderPage() {
  return <Builder />;
}
