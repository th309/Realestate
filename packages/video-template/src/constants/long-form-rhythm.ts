/**
 * Long-form pacing: rotate layout/emphasis every N frames so narration can run
 * for minutes without the viewer staring at one frozen composition (30fps).
 */
export const LONG_FORM_VISUAL_RHYTHM_FRAMES = 540;
