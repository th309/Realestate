# Nixpacks configuration for Railway - NestJS Backend
# Pure Node.js service (Python analytics is now a separate service)

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:backend"]

[start]
cmd = "node packages/backend/dist/main.js"
