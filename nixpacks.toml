# Railpack configuration for Railway - Backend service

[phases.install]
cmds = [
    "npm ci",
    "pip install -r packages/backend/requirements.txt"
]

[phases.build]
cmds = ["npm run build:backend"]

[start]
cmd = "node packages/backend/dist/main.js"
