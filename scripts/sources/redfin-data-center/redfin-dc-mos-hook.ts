import type { SupabaseClient } from "@supabase/supabase-js";

/** Replaced with the real implementation in a later task (Phase 9). */
export async function runMonthsOfSupplyHook(
  _supabase: SupabaseClient,
): Promise<void> {
  console.log("  [redfin-dc] MoS hook stub (no-op until Phase 9).");
}
